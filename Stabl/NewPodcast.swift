//
//  NewPodcast.swift
//  Stabl
//
//  Created by Mijin Cho on 06/03/2016.
//  Copyright © 2016 Mijin Cho. All rights reserved.
//

import Foundation
import CoreData

class NewPodcast: NSManagedObject {

  /*
// Insert code here to add functionality to your managed object subclass
  
    override init(entity: NSEntityDescription, insertInto context: NSManagedObjectContext?) {
        super.init(entity: entity, insertInto: context)
    }
    
    init( artistName : String, collectionId : Int, collectionName : String, title : String, description: String, genres : NSArray,
          duration: Int, enclosure : NSDictionary, feedUrl : String, artworkUrl100 : String, releaseDate : String, collectionPrice : Float, country : String, currency: String, primaryGenreName : String, trackCount:Int, insertInto context: NSManagedObjectContext) {
        
        let entity = NSEntityDescription.entity(forEntityName: "NewPodcast", in: context)!
        super.init(entity: entity, insertInto: context)

        self.collectionId = collectionId as NSNumber?
        self.artistName = artistName
        self.collectionName = collectionName
        self.title = title
        self.desc = description
        //self.genres = genres
        //self.enclosure = enclosure
        self.feedUrl = feedUrl
        self.artworkUrl100 = artworkUrl100
        //self.collectionPrice = collectionPrice
        
        self.duration = duration as NSNumber?
        let dateFormatter = DateFormatter()
        dateFormatter.dateFormat = "yyyy-MM-dd"
        self.releaseDate = releaseDate
        self.country = country
        self.currency = currency
        self.primaryGenreName = primaryGenreName
        
        self.trackCount = trackCount as NSNumber?
    }*/
}
